# =============================================================================
# Environment Variables - PRISM Web Application
# =============================================================================
# Copy this file to .env and fill in your values
# NEVER commit .env to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Required: Authentication & Security
# -----------------------------------------------------------------------------

# Secret key for Flask session encryption (min 32 characters)
SECRET_KEY=your_secret_key_here_at_least_32_characters

# JWT Token Key for authentication
TOKEN_KEY=your_token_key_here

# -----------------------------------------------------------------------------
# Required: Database
# -----------------------------------------------------------------------------

# MongoDB Atlas Connection String
# Format: mongodb+srv://username:password@cluster.mongodb.net/?appName=YourApp
MONGODB_URL=mongodb+srv://username:password@cluster.mongodb.net/?appName=YourApp

# Database Name
DB_NAME=prism_db

# -----------------------------------------------------------------------------
# Railway Deployment (Automatically set by Railway)
# -----------------------------------------------------------------------------

# Port (Railway sets this automatically, default 5001 for local dev)
# PORT=5001

# -----------------------------------------------------------------------------
# Optional: Development Settings
# -----------------------------------------------------------------------------

# Flask Debug Mode (set to "true" only in development, NEVER in production)
FLASK_DEBUG=false

# -----------------------------------------------------------------------------
# Railway Environment Variables to Configure:
# -----------------------------------------------------------------------------
# In Railway Dashboard → Your Project → Variables, add:
#
# 1. SECRET_KEY      → Generate: openssl rand -hex 32
# 2. TOKEN_KEY       → Generate: openssl rand -hex 16
# 3. MONGODB_URL     → Your MongoDB Atlas connection string
# 4. DB_NAME         → Your database name
#
# Railway automatically provides:
# - PORT (injected at runtime)
# - RAILWAY_ENVIRONMENT
# - RAILWAY_SERVICE_NAME
# =============================================================================
